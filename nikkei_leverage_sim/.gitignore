__pycache__/
*.pyc
*.egg-info/
.pytest_cache/
.mypy_cache/
build/
dist/
# Generated data CSVs (regenerate with the `synth`/`fetch` CLI commands)
data/*.csv

# Variant grid: keep summaries / plots / report, drop the regenerable walk-forward
# cache (a self-produced pickle) and the bulky per-row JSON dumps (CSV is canonical).
outputs_variants/wf_params.pkl
outputs_variants/*/rows.json

# Margin-call scenarios: keep summary.json / plots / report, drop the bulky
# regenerable per-row CSVs (regenerate via the CLI / verify_margin_call.py).
outputs_margincall/*/daily.csv
outputs_margincall/*/trades.csv
outputs_margincall/*/optimization.csv

# Local virtualenv
.venv/
