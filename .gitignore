# Python
__pycache__/
*.py[cod]
.pytest_cache/

# Virtual environments
.venv/
venv/

# Backtest data (excluded from repo — provide locally)
nikkei_leverage_sim/data/

# outputs_real/ (full version) and outputs_real_codex/ are committed for the report.
# Old sample run output stays excluded (regenerated locally).
nikkei_leverage_sim/outputs/

# Large raw CSV dumps under outputs_real_fast/ are regenerable and not embedded
# in any report (only the PNGs + summary.json are tracked for REPORT_REAL.md).
nikkei_leverage_sim/outputs_real_fast/*.csv

# Local analysis helper scripts
nikkei_leverage_sim/analyze_daily.py

# Claude Code: track settings.json (shared env/config), ignore transient worktrees + personal overrides
.claude/worktrees/
.claude/settings.local.json
